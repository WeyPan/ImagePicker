package com.lenvar.android.imagepicker;

import android.app.Activity;

class HorizontalImageAdapter extends ImagePickerAdapter {


    public HorizontalImageAdapter(Activity mContext, PickerConfig config) {
        super(mContext, config);
    }
}
